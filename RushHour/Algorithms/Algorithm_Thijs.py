class Algorithm_Thijs:
    def __init__(self, game, history, game_file):
        self.game = game

    def find_move_to_make(self):
        

    def make_move(self, car, direction):
        car = x
        direction = x

        while self.game.is_legal_move(random_car, random_direction) == False:
            random_car, random_direction = self.make_move()
        
        return random_car, random_direction